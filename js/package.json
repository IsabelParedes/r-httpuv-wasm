{
  "name": "@r-httpuv/transport",
  "version": "0.0.0",
  "private": true,
  "description": "Browser transport (httpuv bridge, service worker, virtual shiny socket) for the WASM build of r-httpuv. Built assets are emitted to ../inst/www and installed into R_HOME/library/httpuv/www.",
  "license": "GPL-2.0-or-later",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf ../inst/www"
  },
  "devDependencies": {
    "comlink": "^4.4.2",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
